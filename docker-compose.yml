version: "3.9"

services:
  web:
    image: chuvanhuy/nsa-backend:latest
    container_name: nsa-agent
    ports:
      - "8014:8014"
    restart: unless-stopped
    env_file:
      - .env
    environment:
      PORT: ${PORT:-8014}
