# ===========================================
# neu-scholar-backend — Docker Compose PRODUCTION
# ===========================================
# Chạy: docker compose -f docker-compose.yml -f docker-compose.prod.yml up -d
# Hoặc: docker compose --profile prod up -d (nếu dùng profiles)

services:
  backend:
    build:
      target: production
    volumes: []  # Không mount code trong prod
    restart: always
